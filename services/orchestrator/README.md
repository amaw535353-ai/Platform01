# Orchestrator
The explicit state path is `authenticate -> retrieve -> propose -> authorize -> approve -> execute -> audit`. The model adapter receives retrieved text but no credentials; trusted `packages/zt_platform` code constructs policy input and dispatches immutable registry entries.

