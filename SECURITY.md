# Security policy
Do not use real data or credentials. Report issues privately to repository maintainers. The `lab-vulnerable` profile is intentionally unsafe, loopback-only, synthetic, and must never be exposed or used with hardened data. This project is bounded demonstration evidence, not a certification.

