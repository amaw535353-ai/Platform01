# Security logging boundary
The deterministic hash-chain/redaction implementation is in `packages/zt_platform/core.py`; exporters must remove prompt, retrieved text, credentials, and direct identifiers.

