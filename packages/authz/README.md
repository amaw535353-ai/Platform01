# Authorization package boundary
Token claim validation and the immutable tool registry currently live in `packages/zt_platform/core.py`; this directory reserves the external JWT/JWKS adapter boundary.

