# Placeholders only. Generate local values; never commit populated files.
SESSION_SECRET=generate-a-random-development-value
OIDC_ISSUER=http://127.0.0.1:8080/realms/support
MCP_AUDIENCE=mcp-server
DATABASE_URL=postgresql://app_runtime:generate-locally@postgres/support
ALLOWED_EGRESS_HOSTS=id.example.invalid

